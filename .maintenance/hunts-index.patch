--- a/index.html
+++ b/index.html
@@ -480,6 +480,12 @@
         min-height: 1.4rem;
         margin: 0.7rem 0 0;
       }
+
+      #painel-caca-milicia { width: min(42rem, calc(100vw - 1.5rem)); max-height: 88dvh; overflow-y: auto; }
+      #painel-caca-milicia p { line-height: 1.45; color: #d4cbb4; }
+      #painel-caca-milicia label { display: flex; flex-direction: column; gap: 0.35rem; flex: 1 1 10rem; }
+      #painel-caca-milicia input, #painel-caca-milicia select { width: 100%; min-height: 44px; border: 1px solid #8f7a4c; border-radius: 0.3rem; padding: 0.5rem; background: #171812; color: #fff9df; font: inherit; }
+      #painel-caca-milicia :focus-visible { outline: 3px solid #f1cc69; outline-offset: 2px; }
 
       .inspecao-edificio-status {
         display: inline-block;
@@ -714,6 +720,7 @@
         <button id="abrir-painel-colonia" class="acao-colonia secundario" type="button">
           Painel colonial
         </button>
+        <button id="abrir-caca-milicia" class="acao-colonia secundario" type="button">Caçada da milícia</button>
         <button id="abrir-registros" class="acao-colonia secundario" type="button">
           Registros
         </button>
@@ -1475,6 +1482,27 @@
           role="status"
           aria-live="polite"
         ></p>
+      </section>
+    </dialog>
+
+    <dialog id="painel-caca-milicia" aria-labelledby="titulo-caca-milicia">
+      <section class="loja-conteudo">
+        <header class="loja-cabecalho">
+          <h2 id="titulo-caca-milicia">Expedição de caça da milícia</h2>
+          <button id="fechar-caca-milicia" class="secundario" type="button">Fechar</button>
+        </header>
+        <p>O ponto de encontro fica no noroeste, acima da cabana dos caçadores. A equipe se reúne, sai do mapa, caça e volta com animais, carne e peles.</p>
+        <div class="acoes-transferencia">
+          <label for="equipe-caca-milicia">Milicianos <input id="equipe-caca-milicia" type="number" min="1" max="10" step="1" value="1" inputmode="numeric"></label>
+          <label for="duracao-caca-milicia">Tempo de caça <select id="duracao-caca-milicia"><option value="60">Curta · 60 s</option><option value="120" selected>Normal · 120 s</option><option value="240">Longa · 240 s</option></select></label>
+        </div>
+        <p>Tempos de jogo: 45 s para reunir, 18 s para sair, 18 s para voltar, 8 s para descarregar e 45 s para retornar ao pátio, além da caça. Pausa e velocidade também controlam a expedição.</p>
+        <p>A equipe continua recebendo soldo, mas não conta na segurança da cidade enquanto estiver fora. Grupos maiores e caçadas longas tendem a render mais; a sorte pode mudar bastante o resultado.</p>
+        <button id="iniciar-caca-milicia" type="button">Iniciar uma caça</button>
+        <p id="status-caca-milicia" role="status" aria-live="polite"></p>
+        <p id="carga-caca-milicia"></p>
+        <p>Os animais trazidos são processados por açougueiros (até 2 por ciclo; cada animal rende 4 carnes e 2 peles). A carne segue nas carroças da cabana ao açougue; as peles vão ao estoque existente. O excesso aguarda espaço, sem desaparecer. Escoe a carga anterior antes de outra caçada.</p>
+        <p id="resultado-caca-milicia"></p>
       </section>
     </dialog>
 
@@ -2260,6 +2288,7 @@
         largura: 600,
         altura: 260,
       };
+      const areaPontoCacaMilicia = { x: 440, y: 150, largura: 620, altura: 250 };
       const areaAcougueColonia = {
         x: 4500,
         y: 4550,
@@ -3422,6 +3451,7 @@
         trechoEstradaEmObra: null,
         saneamentoConstruido: false,
         quantidadePostosGuarda: 0,
+        cacaMilicia: novoEstadoCacaMilicia(),
         quantidadeMilicianos: 0,
         arqueirosMilicia: 0,
         armasMetalMilicia: 0,
@@ -3538,6 +3568,251 @@
         [1320, 520, 26],
         [65, 330, 29],
       ];
+
+      // MILITIA_HUNT_EXPEDITIONS_V1: simulation-time, save-safe expeditions.
+      const fasesCacaMilicia = ["reunindo", "saindo", "cacando", "voltando", "entregando", "retornando"];
+      const nomesFasesCacaMilicia = { reunindo: "Reunindo a milícia", saindo: "Partindo para a floresta", cacando: "Caçando fora do mapa", voltando: "Voltando com a caça", entregando: "Descarregando na cabana", retornando: "Retornando ao pátio" };
+      const painelCacaMilicia = document.querySelector("#painel-caca-milicia");
+      const equipeCacaMiliciaEl = document.querySelector("#equipe-caca-milicia");
+      const duracaoCacaMiliciaEl = document.querySelector("#duracao-caca-milicia");
+      const statusCacaMiliciaEl = document.querySelector("#status-caca-milicia");
+      const cargaCacaMiliciaEl = document.querySelector("#carga-caca-milicia");
+      const resultadoCacaMiliciaEl = document.querySelector("#resultado-caca-milicia");
+      const iniciarCacaMiliciaBotao = document.querySelector("#iniciar-caca-milicia");
+
+      function novoEstadoCacaMilicia() {
+        return { expedicao: null, carga: { animais: 0, carne: 0, peles: 0 }, ultima: null, concluidas: 0 };
+      }
+
+      function normalizarCacaMilicia(dados) {
+        const novo = novoEstadoCacaMilicia();
+        if (!dados || typeof dados !== "object") return novo;
+        // A single expedition has bounded yields; pending cargo is never silently discarded.
+        for (const [chave, limite] of Object.entries({ animais: 24, carne: 320, peles: 160 })) {
+          novo.carga[chave] = numeroValido(dados.carga?.[chave], 0, limite, 0);
+          if (chave === "animais") novo.carga[chave] = Math.floor(novo.carga[chave]);
+        }
+        novo.concluidas = Math.floor(numeroValido(dados.concluidas, 0, 1000000, 0));
+        if (dados.ultima && typeof dados.ultima === "object") {
+          novo.ultima = {};
+          for (const [chave, limite] of Object.entries({ animais: 24, carne: 320, peles: 160, quantidade: 10, segundos: 240 })) {
+            novo.ultima[chave] = numeroValido(dados.ultima[chave], 0, limite, 0);
+          }
+        }
+        const e = dados.expedicao;
+        if (e && fasesCacaMilicia.includes(e.fase) && estado.coloniaIniciada && estado.cabanaCacadoresConstruida) {
+          const quantidade = Math.floor(numeroValido(e.quantidade, 0, Math.min(10, estado.quantidadeMilicianos), 0));
+          if (quantidade > 0) {
+            const duracao = [60, 120, 240].includes(e.duracao) ? e.duracao : 120;
+            novo.expedicao = {
+              fase: e.fase, quantidade, duracao,
+              tempo: numeroValido(e.tempo, 0, e.fase === "cacando" ? duracao : duracaoFaseCacaMilicia(e.fase), 0),
+              semente: Math.floor(numeroValido(e.semente, 1, 4294967295, 1)),
+              esforco: numeroValido(e.esforco, 0, quantidade * duracao, 0),
+            };
+            // Earlier phases cannot carry credit for hunting that has not happened.
+            if (["reunindo", "saindo"].includes(e.fase)) novo.expedicao.esforco = 0;
+          }
+        }
+        return novo;
+      }
+
+      function duracaoFaseCacaMilicia(fase, e = estado.cacaMilicia.expedicao) {
+        return fase === "cacando" ? (e?.duracao || 120) : ({ reunindo: 45, saindo: 18, voltando: 18, entregando: 8, retornando: 45 }[fase] || 1);
+      }
+
+      function milicianosEmCacaColonia() {
+        return Math.min(estado.quantidadeMilicianos, estado.cacaMilicia.expedicao?.quantidade || 0);
+      }
+
+      function resultadoExpedicaoMilicia(esforco, semente) {
+        let s = (semente >>> 0) || 1;
+        const aleatorio = () => { s = (Math.imul(s, 1664525) + 1013904223) >>> 0; return s / 4294967296; };
+        const evento = aleatorio();
+        const abundancia = evento < 0.08 ? 0.12 : evento > 0.9 ? 2.5 : 0.35 + aleatorio() * 1.55;
+        const base = numeroValido(esforco, 0, 2400, 0) / 60 * abundancia;
+        return {
+          animais: Math.min(24, Math.floor(base * (0.08 + aleatorio() * 0.1))),
+          carne: Math.min(320, Math.floor(base * (1.1 + aleatorio() * 0.7))),
+          peles: Math.min(160, Math.floor(base * (0.35 + aleatorio() * 0.3))),
+        };
+      }
+
+      function motivoBloqueioCacaMilicia() {
+        if (!estado.coloniaIniciada || !estado.cabanaCacadoresConstruida) return "Construa a cabana dos caçadores.";
+        if (estado.quantidadeMilicianos < 1) return "Aguarde a contratação de pelo menos um miliciano.";
+        if (estado.cacaMilicia.expedicao) return "Uma expedição já está em andamento.";
+        if (Object.values(estado.cacaMilicia.carga).some((n) => n > 0.000001)) return "Aguarde o escoamento da carga anterior: transportadores, espaço para peles e açougueiros para os animais.";
+        if (estado.ciclosSoldosAtrasados > 0) return "Regularize os soldos antes de iniciar outra caçada.";
+        return "";
+      }
+
+      function iniciarExpedicaoMilicia(quantidade = Number(equipeCacaMiliciaEl.value), duracao = Number(duracaoCacaMiliciaEl.value)) {
+        const motivo = motivoBloqueioCacaMilicia();
+        if (motivo) { definirMensagem(motivo); atualizarPainelCacaMilicia(); return false; }
+        if (!Number.isInteger(quantidade) || quantidade < 1 || quantidade > Math.min(10, estado.quantidadeMilicianos) || ![60, 120, 240].includes(duracao)) {
+          definirMensagem("Escolha de 1 até o efetivo disponível e uma duração válida.");
+          return false;
+        }
+        estado.cacaMilicia.expedicao = { fase: "reunindo", quantidade, duracao, tempo: 0, esforco: 0, semente: 1 + Math.floor(Math.random() * 4294967295) };
+        definirMensagem(`${quantidade} milicianos se reúnem junto à cabana. A caça durará ${duracao} segundos de jogo, além dos deslocamentos.`);
+        atualizarInterface();
+        salvarProgresso();
+        return true;
+      }
+
+      function escoarCargaCacaMilicia() {
+        const carga = estado.cacaMilicia.carga;
+        const carne = Math.min(carga.carne, Math.max(0, capacidadeEstoqueLocalCarneSelvagemColonia - estado.estoqueLocalCarneSelvagem));
+        const peles = Math.min(carga.peles, Math.max(0, capacidadeArmazemInternoColonia(capacidadeCouroColonia) - estado.estoqueCouro));
+        estado.estoqueLocalCarneSelvagem += carne;
+        estado.estoqueCouro += peles;
+        carga.carne -= carne;
+        carga.peles -= peles;
+      }
+
+      function operacoesAnimaisCacaMilicia(capacidade) {
+        const carga = estado.cacaMilicia.carga;
+        return estado.acougueConstruido ? Math.min(2, Math.floor(capacidade || 0), Math.floor(carga.animais), Math.floor((320 - carga.carne) / 4), Math.floor((160 - carga.peles) / 2)) : 0;
+      }
+
+      function processarAnimaisCacaMilicia(empregos) {
+        const carga = estado.cacaMilicia.carga;
+        // Whole animals consume butcher operations, not free additional workforce.
+        const operacoes = operacoesAnimaisCacaMilicia(empregos.acougue);
+        carga.animais -= operacoes;
+        carga.carne += operacoes * 4;
+        carga.peles += operacoes * 2;
+        escoarCargaCacaMilicia();
+        return operacoes;
+      }
+
+      function atualizarExpedicaoMilicia(delta) {
+        if (!estado.coloniaIniciada || estado.jogoPausado || !Number.isFinite(delta) || delta <= 0) return;
+        escoarCargaCacaMilicia();
+        let restante = delta;
+        let mudou = false;
+        // At most six transitions; no wall-clock catch-up or repeated rewards on load.
+        for (let i = 0; i < 7 && restante > 0 && estado.cacaMilicia.expedicao; i += 1) {
+          const e = estado.cacaMilicia.expedicao;
+          e.quantidade = Math.min(e.quantidade, estado.quantidadeMilicianos);
+          if (e.quantidade <= 0) { estado.cacaMilicia.expedicao = null; mudou = true; break; }
+          const limite = duracaoFaseCacaMilicia(e.fase, e);
+          const passo = Math.min(restante, Math.max(0, limite - e.tempo));
+          e.tempo += passo;
+          restante -= passo;
+          if (e.fase === "cacando") e.esforco += e.quantidade * passo;
+          if (e.tempo + 0.0000001 < limite) break;
+          mudou = true;
+          if (e.fase === "entregando") {
+            const resultado = resultadoExpedicaoMilicia(e.esforco, e.semente);
+            for (const chave of ["animais", "carne", "peles"]) estado.cacaMilicia.carga[chave] += resultado[chave];
+            estado.cacaMilicia.ultima = { ...resultado, quantidade: e.quantidade, segundos: e.esforco / e.quantidade };
+            estado.cacaMilicia.concluidas += 1;
+            definirMensagem(`Caçada concluída: ${resultado.animais} animais, ${resultado.carne} carnes e ${resultado.peles} peles. A carga excedente permanece na cabana até haver espaço.`);
+            escoarCargaCacaMilicia();
+          }
+          const proxima = fasesCacaMilicia[fasesCacaMilicia.indexOf(e.fase) + 1];
+          if (proxima) { e.fase = proxima; e.tempo = 0; }
+          else estado.cacaMilicia.expedicao = null;
+        }
+        if (mudou) { atualizarInterface(); salvarProgresso(); }
+        else if (painelCacaMilicia.open) atualizarPainelCacaMilicia();
+      }
+
+      function atualizarPainelCacaMilicia() {
+        const e = estado.cacaMilicia.expedicao;
+        const n = Math.min(10, estado.quantidadeMilicianos);
+        equipeCacaMiliciaEl.max = String(Math.max(1, n));
+        equipeCacaMiliciaEl.disabled = Boolean(e) || n === 0;
+        duracaoCacaMiliciaEl.disabled = Boolean(e);
+        iniciarCacaMiliciaBotao.disabled = Boolean(motivoBloqueioCacaMilicia());
+        const presente = Math.max(0, n - milicianosEmCacaColonia());
+        statusCacaMiliciaEl.textContent = e
+          ? `${nomesFasesCacaMilicia[e.fase]} · ${e.quantidade} milicianos · ${Math.ceil(Math.max(0, duracaoFaseCacaMilicia(e.fase, e) - e.tempo))} s nesta etapa${estado.jogoPausado ? " · PAUSADO" : ""}. ${presente} milicianos permanecem na cidade.`
+          : (motivoBloqueioCacaMilicia() || `${n} milicianos disponíveis. Escolha o tamanho da equipe e inicie a caça.`);
+        const c = estado.cacaMilicia.carga;
+        cargaCacaMiliciaEl.textContent = `Carga aguardando: ${formatarQuantidade(c.animais)} animais · ${formatarQuantidade(c.carne)} carnes · ${formatarQuantidade(c.peles)} peles. Carne na origem: ${formatarQuantidade(estado.estoqueLocalCarneSelvagem)} / ${capacidadeEstoqueLocalCarneSelvagemColonia}.`;
+        const u = estado.cacaMilicia.ultima;
+        resultadoCacaMiliciaEl.textContent = u
+          ? `Última caçada: ${u.quantidade} milicianos, ${Math.round(u.segundos)} s de caça; ${u.animais} animais, ${u.carne} carnes e ${u.peles} peles. Expedições concluídas: ${estado.cacaMilicia.concluidas}.`
+          : "Nenhuma expedição concluída. Resultados podem variar bastante, inclusive caçadas quase vazias.";
+      }
+
+      function abrirPainelCacaMilicia() {
+        const e = estado.cacaMilicia.expedicao;
+        equipeCacaMiliciaEl.value = String(e?.quantidade || Math.max(1, Math.min(10, estado.quantidadeMilicianos)));
+        duracaoCacaMiliciaEl.value = String(e?.duracao || 120);
+        atualizarPainelCacaMilicia();
+        if (typeof painelCacaMilicia.showModal === "function") { if (!painelCacaMilicia.open) painelCacaMilicia.showModal(); }
+        else painelCacaMilicia.setAttribute("open", "");
+      }
+
+      function posicaoPatioMiliciano(indice) {
+        return { x: areaPatioTreinoColonia.x + 300 + (indice % 5) * 75, y: areaPatioTreinoColonia.y + 285 + Math.floor(indice / 5) * 120 };
+      }
+
+      function pontoReuniaoMiliciano(indice) {
+        return { x: areaPontoCacaMilicia.x + 160 + (indice % 5) * 65, y: areaPontoCacaMilicia.y + 110 + Math.floor(indice / 5) * 60 };
+      }
+
+      function posicaoMilicianoExpedicao(indice) {
+        const e = estado.cacaMilicia.expedicao;
+        if (!e || indice >= e.quantidade) return posicaoPatioMiliciano(indice);
+        if (e.fase === "cacando") return null;
+        const ponto = pontoReuniaoMiliciano(indice);
+        const progresso = Math.min(1, e.tempo / duracaoFaseCacaMilicia(e.fase, e));
+        if (e.fase === "entregando") return ponto;
+        if (["reunindo", "retornando"].includes(e.fase)) {
+          // Road routing keeps the long approach out of buildings and fortifications.
+          const caminho = [...rotaMaisRapidaColonia(posicaoPatioMiliciano(indice), { x: 240, y: 2230 }).pontos, { x: 240, y: 430 }, { x: ponto.x, y: 430 }, ponto];
+          return percorrerCaminhoPorDistancia(caminho, e.fase === "retornando" ? 1 - progresso : progresso);
+        }
+        const caminho = [ponto, { x: 290 + indice * 18, y: 190 }, { x: 170 + indice * 16, y: 80 }, { x: 110 + indice * 13, y: -220 - indice * 30 }];
+        return percorrerCaminhoPorDistancia(caminho, e.fase === "voltando" ? 1 - progresso : progresso);
+      }
+
+      function desenharPontoCacaMilicia() {
+        const { x, y, largura, altura } = areaPontoCacaMilicia;
+        contexto.save();
+        contexto.fillStyle = estado.cabanaCacadoresConstruida ? "#c4ad74" : "#89946c";
+        contexto.fillRect(x, y, largura, altura);
+        contexto.strokeStyle = "#57462e";
+        contexto.lineWidth = 7;
+        contexto.strokeRect(x, y, largura, altura);
+        contexto.fillStyle = "#55452b";
+        contexto.fillRect(x + 50, y + 40, 8, 125);
+        contexto.fillStyle = "#8f3431";
+        contexto.fillRect(x + 58, y + 40, 90, 50);
+        contexto.fillStyle = "#30291f";
+        contexto.font = "bold 24px sans-serif";
+        contexto.textAlign = "center";
+        contexto.fillText("PONTO DE CAÇA · MILÍCIA", x + largura / 2, y + 30);
+        contexto.font = "21px sans-serif";
+        contexto.fillText(estado.cacaMilicia.expedicao ? nomesFasesCacaMilicia[estado.cacaMilicia.expedicao.fase] : "Toque para iniciar uma caça", x + largura / 2, y + altura - 22);
+        contexto.restore();
+      }
+
+      function desenharCargaExpedicaoMilicia() {
+        const e = estado.cacaMilicia.expedicao;
+        if (!e || !["voltando", "entregando"].includes(e.fase)) return;
+        const p = posicaoMilicianoExpedicao(0);
+        if (!p) return;
+        const carga = resultadoExpedicaoMilicia(e.esforco, e.semente);
+        contexto.save();
+        for (let i = 0; i < Math.min(6, carga.animais); i += 1) {
+          const x = p.x + 70 + (i % 3) * 58;
+          const y = p.y + 75 + Math.floor(i / 3) * 45;
+          contexto.fillStyle = "#745132";
+          contexto.fillRect(x, y, 36, 19);
+          contexto.fillRect(x + 28, y - 10, 13, 15);
+          contexto.fillRect(x + 3, y + 17, 5, 12);
+          contexto.fillRect(x + 27, y + 17, 5, 12);
+        }
+        if (carga.carne > 0) { contexto.fillStyle = "#a85447"; contexto.fillRect(p.x - 50, p.y + 35, 32, 26); }
+        if (carga.peles > 0) { contexto.fillStyle = "#b9915c"; contexto.fillRect(p.x - 90, p.y + 37, 32, 22); }
+        contexto.restore();
+      }
 
       function definirMensagem(texto) {
         mensagemEl.textContent = texto;
@@ -4922,6 +5197,7 @@
         estado.trechoEstradaEmObra = null;
         estado.saneamentoConstruido = false;
         estado.quantidadePostosGuarda = 0;
+        estado.cacaMilicia = novoEstadoCacaMilicia();
         estado.quantidadeMilicianos = 0;
         estado.arqueirosMilicia = 0;
         estado.armasMetalMilicia = 0;
@@ -5053,6 +5329,7 @@
                 estado.segundoBlocoMoradiasConstruido,
               terceiroBlocoMoradiasConstruido:
                 estado.terceiroBlocoMoradiasConstruido,
+              cacaMilicia: estado.cacaMilicia,
               cadeiaCarne: {
                 cabanaCacadores: estado.cabanaCacadoresConstruida,
                 acougue: estado.acougueConstruido,
@@ -6775,6 +7052,7 @@
           if (estado.coloniaIniciada) {
             estado.mapaAtual = "expansao";
           }
+          estado.cacaMilicia = normalizarCacaMilicia(salvo.expansao?.economia?.cacaMilicia);
           const coloniaReiniciadaAoCarregar =
             estado.mapaAtual === "expansao" &&
             estado.revisaoColonia < versaoColonia;
@@ -7020,6 +7298,7 @@
         estado.trechoEstradaEmObra = null;
         estado.saneamentoConstruido = false;
         estado.quantidadePostosGuarda = 0;
+        estado.cacaMilicia = novoEstadoCacaMilicia();
         estado.quantidadeMilicianos = 0;
         estado.arqueirosMilicia = 0;
         estado.armasMetalMilicia = 0;
@@ -7299,6 +7578,9 @@
           Math.max(0, alvoPastoresExpandidos - empregos.pastagem),
           limiteFolhaAlimentar,
         );
+        if (estado.cacaMilicia.carga.animais > 0 && estado.acougueConstruido) {
+          adicionarComLimite("acougue", 2, Math.max(0, 2 - empregos.acougue), limiteFolhaAlimentar);
+        }
         const carneCruaArmazenada =
           estado.estoqueCarneSelvagem + estado.estoqueCarneCriacao;
         const espacoRefeicoesCarne = Math.max(
@@ -7845,7 +8127,7 @@
           Math.max(0, capacidadeTransporte - cargaMedicinalPrioritaria),
         );
         const operacoesAcougue = Math.min(
-          eficiente(empregos.acougue || 0, "acougue"),
+          Math.max(0, eficiente(empregos.acougue || 0, "acougue") - operacoesAnimaisCacaMilicia(eficiente(empregos.acougue || 0, "acougue"))),
           Math.floor(carnesCruas / carneCruaPorOperacaoAcougue),
         );
         const cortes = operacoesAcougue * cortesPorOperacaoAcougue;
@@ -8790,7 +9072,7 @@
         return Math.min(
           100,
           Math.round(
-            (estado.quantidadeMilicianos * 3 +
+            ((estado.quantidadeMilicianos - milicianosEmCacaColonia()) * 3 +
               estado.quantidadeGuardas * 6 +
               estado.quantidadePostosGuarda * 5) *
               fatorAtraso,
@@ -9675,6 +9957,7 @@
       }
 
       function atualizarInterface() {
+        atualizarPainelCacaMilicia();
         const noPostoAvancado = estado.mapaAtual === "posto";
         const noAssentamento =
           noPostoAvancado && estado.local === "assentamento";
@@ -10025,7 +10308,7 @@
         trabalhoFeiranteEl.textContent =
           `${empregos.feirante} / ${maximoFeirantesColonia} · ${festivalAtivoColonia() ? "festival ativo" : "mercado regular"}`;
         trabalhoGeralEl.textContent = String(empregos.geral);
-        defesaMiliciaEl.textContent = `${textoEquipamentoDefesaColonia("milicia")} · ${textoArmaduraDefesaColonia("milicia")}`;
+        defesaMiliciaEl.textContent = `${textoEquipamentoDefesaColonia("milicia")} · ${textoArmaduraDefesaColonia("milicia")}${milicianosEmCacaColonia() ? ` · ${milicianosEmCacaColonia()} em caça` : ""}`;
         defesaGuardasEl.textContent = `${textoEquipamentoDefesaColonia("guarda")} · ${textoArmaduraDefesaColonia("guarda")}`;
         defesaSoldadosEl.textContent = `${textoEquipamentoDefesaColonia("soldado")} · ${textoArmaduraDefesaColonia("soldado")}`;
         defesaSegurancaEl.textContent = `${segurancaCivil} / 100`;
@@ -10558,7 +10841,7 @@
           `+${empregos.pedreira * pedraPorTrabalhadorPedreira} pedra`;
         registrosEls["producao-metal"].textContent =
           `${empregos.ferraria} ferramentas possíveis · ${empregos.forja} armas possíveis`;
-        registrosEls.milicia.textContent = `${textoEquipamentoDefesaColonia("milicia")} · ${textoArmaduraDefesaColonia("milicia")}`;
+        registrosEls.milicia.textContent = `${textoEquipamentoDefesaColonia("milicia")} · ${textoArmaduraDefesaColonia("milicia")}${milicianosEmCacaColonia() ? ` · ${milicianosEmCacaColonia()} em caça` : ""}`;
         registrosEls.guardas.textContent = `${textoEquipamentoDefesaColonia("guarda")} · ${textoArmaduraDefesaColonia("guarda")}`;
         registrosEls.soldados.textContent = `${textoEquipamentoDefesaColonia("soldado")} · ${textoArmaduraDefesaColonia("soldado")}`;
         registrosEls["seguranca-civil"].textContent = `${segurancaCivil} / 100`;
@@ -11561,6 +11844,7 @@
           const ocupados = Math.min(trabalhadoresPorCabanaColeta, Math.max(0, (empregos.coletor || 0) - indice * trabalhadoresPorCabanaColeta));
           adicionar(`coleta-${indice + 1}`, `Cabana de coleta ${indice + 1}`, area, "cabanaColeta", estado.quantidadeCabanasColeta > indice, "Coletores juntam ervas medicinais e as deixam na origem para transporte.", { emprego: emprego("coletor", trabalhadoresPorCabanaColeta, ocupados), producao: ritmo("coletor", ervasPorColetor, "ervas", ocupados), inventario: [estoque("Ervas nas cabanas", estado.estoqueLocalErvas, capacidadeEstoqueLocalErvasColonia)], operacao: [manutencao("cabanaColeta")] });
         });
+        adicionar("expedicao-milicia", "Ponto de caça da milícia", areaPontoCacaMilicia, null, estado.cabanaCacadoresConstruida, "Reúna a milícia e organize uma expedição fora do mapa.", { empregados: [linhaInspecaoColonia("Na expedição", milicianosEmCacaColonia())], inventario: [estoque("Animais aguardando", estado.cacaMilicia.carga.animais, 24), estoque("Carne aguardando", estado.cacaMilicia.carga.carne, 320), estoque("Peles aguardando", estado.cacaMilicia.carga.peles, 160)] });
         adicionar("cacadores", "Cabana dos caçadores", areaCabanaCacadores, "cabanaCacadores", estado.cabanaCacadoresConstruida, "Caçadores recolhem carne selvagem e peles na floresta do noroeste.", { emprego: emprego("cacador", maximoTrabalhadoresCacadores), producao: [...ritmo("cacador", carneSelvagemPorCacador, "carnes"), linhaInspecaoColonia("Peles potenciais", `${formatarQuantidade((empregos.cacador || 0) * couroPorCacador)} / 60 s`)], inventario: [estoque("Carne na origem", estado.estoqueLocalCarneSelvagem, capacidadeEstoqueLocalCarneSelvagemColonia), estoque("Carne armazenada", estado.estoqueCarneSelvagem, capacidadeCarneSelvagemColonia)], operacao: [manutencao("cabanaCacadores")] });
         adicionar("deposito-madeira", "Depósito de madeira", areaDepositoMadeira, "depositoMadeira", estado.depositoMadeiraConstruido, "Recebe das carroças a madeira usada por oficinas, navios e construções.", { inventario: [estoque("Madeira", estado.estoqueMadeira, capacidadeMadeiraColonia), estoque("Aguardando transporte", estado.estoqueLocalMadeira, capacidadeEstoqueLocalMadeiraColonia)], operacao: [linhaInspecaoColonia("Transportadores", `${empregos.transportador || 0}/${maximoTransportadoresColonia}`), manutencao("depositoMadeira")] });
         adicionar("ervario", "Ervário", areaErvario, "ervario", estado.ervarioConstruido, "Armazena ervas entregues pelas carroças antes do uso clínico.", { inventario: [estoque("Ervas", estado.estoqueErvas, interno(capacidadeErvasColonia)), estoque("Aguardando transporte", estado.estoqueLocalErvas, capacidadeEstoqueLocalErvasColonia)], operacao: [linhaInspecaoColonia("Prioridade", estado.saudeColonia < saudePrioridadeEmergencialColonia ? "Medicamentos urgentes" : "Reserva sanitária"), manutencao("ervario")] });
@@ -11739,7 +12023,8 @@
           folga,
         );
         if (!edificio) return false;
-        abrirInspecaoEdificioColonia(edificio);
+        if (edificio.id === "expedicao-milicia") abrirPainelCacaMilicia();
+        else abrirInspecaoEdificioColonia(edificio);
         return true;
       }
 
@@ -13051,8 +13336,11 @@
           estado.estoqueCouro += novoCouro;
           couroProduzido += novoCouro;
 
+          const animaisProcessadosCaca = processarAnimaisCacaMilicia({ ...empregos, acougue: trabalhoEficiente(empregos.acougue || 0, "acougue") });
+          carneSelvagemProduzida += animaisProcessadosCaca * 4;
+          couroProduzido += animaisProcessadosCaca * 2;
           const operacoesAcougue = Math.min(
-            trabalhoEficiente(empregos.acougue || 0, "acougue"),
+            Math.max(0, trabalhoEficiente(empregos.acougue || 0, "acougue") - animaisProcessadosCaca),
             Math.floor(
               (estado.estoqueCarneSelvagem + estado.estoqueCarneCriacao) /
                 carneCruaPorOperacaoAcougue,
@@ -18536,6 +18824,11 @@
 
       function desenharEfetivosDefesaColonia() {
         for (let indice = 0; indice < estado.quantidadeMilicianos; indice += 1) {
+          if (indice < milicianosEmCacaColonia()) {
+            const ponto = posicaoMilicianoExpedicao(indice);
+            if (ponto) desenharEfetivoDefesaColonia(ponto.x, ponto.y, "#6f7a3f", tipoEquipamentoEfetivoColonia("milicia", indice));
+            continue;
+          }
           const coluna = indice % 5;
           const linha = Math.floor(indice / 5);
           const x = areaPatioTreinoColonia.x + 300 + coluna * 75;
@@ -18644,7 +18937,9 @@
         desenharEstruturasDefesaColonia();
         desenharColonosNovaColonia();
         desenharJovensColonia();
+        desenharPontoCacaMilicia();
         desenharEfetivosDefesaColonia();
+        desenharCargaExpedicaoMilicia();
       }
 
       function interpolarPonto(inicio, fim, progresso) {
@@ -19335,6 +19630,7 @@
         atualizarPrioridadesColonia(deltaSimulacao);
         atualizarNecessidadesColonia(deltaSimulacao);
         atualizarDefesaColonia(deltaSimulacao);
+        atualizarExpedicaoMilicia(deltaSimulacao);
         atualizarConstrucaoColonia(deltaSimulacao);
         if (
           estado.local === "floresta" ||
@@ -19359,6 +19655,12 @@
         requestAnimationFrame(quadro);
       }
 
+      document.querySelector("#abrir-caca-milicia").addEventListener("click", abrirPainelCacaMilicia);
+      iniciarCacaMiliciaBotao.addEventListener("click", () => iniciarExpedicaoMilicia());
+      document.querySelector("#fechar-caca-milicia").addEventListener("click", () => {
+        if (typeof painelCacaMilicia.close === "function") painelCacaMilicia.close();
+        else painelCacaMilicia.removeAttribute("open");
+      });
       cacarBotao.addEventListener("click", iniciarCacada);
       voltarBotao.addEventListener("click", voltarAoAssentamento);
       descansarBotao.addEventListener("click", descansar);
